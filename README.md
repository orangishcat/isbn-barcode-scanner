# ISBN Barcode Scanner

A simple script that I vibecoded a year ago. It uses `zxing` in your browser to
scan ISBN barcodes, then uses the `Google Books API` to fetch information about
the ISBN. Then it adds that info into your spreadsheet.
